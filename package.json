{
  "name": "decision-bot",
  "version": "1.0.0",
  "description": "A bot for making decisions",
  "main": "./src/index.js",
  "scripts": {
    "start": "node ./src/index.js",
    "update": "node ./src/deploy-commands.js",
    "deploy": "flyctl deploy",
    "build": "npm run update && npm run deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@discordjs/formatters": "^0.3.0",
    "discord.js": "^14.9.0",
    "dotenv": "^16.0.3",
    "undici": "^5.21.0"
  }
}
