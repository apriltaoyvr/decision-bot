{
  "name": "decision-bot",
  "version": "1.0.0",
  "description": "A bot for making decisions",
  "main": "./src/index.js",
  "scripts": {
    "start": "node ./src/index.js",
    "update": "node ./src/deploy-commands.js",
    "deploy": "flyctl deploy",
    "build": "pnpm run update && pnpm run deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@discordjs/formatters": "^0.3.2",
    "discord.js": "^14.13.0",
    "dotenv": "^16.3.1",
    "undici": "^5.24.0"
  }
}
